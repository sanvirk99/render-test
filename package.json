{
  "name": "part3",
  "version": "1.0.0",
  "description": "part3 node",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd /Users/san/full-stack-open/part2/phonebook && npm run build && cp -r dist /Users/san/full-stack-open-part3",
    "deploy:full": "npm run build:ui && cd /Users/san/full-stack-open-part3 && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
